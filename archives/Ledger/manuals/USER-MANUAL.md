# Ledger notes

Private room. Sign in on 0?0, then `OPEN LEDGER`.

Add shows, links, or notes in `data.json` when you want.
A second steward account can be granted later without making the room public.

On 0?0, **MAP** lists this room with `files:` as private `data.json` (no public
stream catalog). iNi is off unless you opt in. Commons operator tools
(MONITOR / markers) do not rewrite this folder; they only change registry
markers if ever used.
