# About uXu

Tags: about, uxu, commons, site, repo, app, 0q0, ini
Source: interactive twin — ABOUT on the 0?0 archive interface

## What uXu is

**uXu** is the shared archive commons — the **site**, the **repo**, and the
installable **app**. Independent archives keep their own voice, stack, and
curation. Nobody takes your collection away from you.

| Piece | Role |
|-------|------|
| **uXu site** | Live commons landing — https://rasvibir.github.io/uXu/ |
| **uXu repo** | github.com/RASvibir/uXu — software, templates, docs (MIT) |
| **uXu app** | Installable PWA invite (wordmark **uXu**, not 0?0) |
| **Child archives** | Each its own room (e.g. CyberCat Sunflower) |

Registration is not ownership. Creators choose a home link style: public **uXu**
chip or console **← 0?0** back control.

## 0?0 — the archive interface

**0?0** (`0?0.uXu.0000`) is the *root archive interface* inside uXu — the CRT
console. It is **not** the brand of the site or the app.

Inside the interface:

- **Registry** — forever ids (`Title.uXu.NNNN`). Placemarkers keep history honest.
- **Open** — enter archives that have a real page.
- **Quick Nav** — ABOUT · CREATE · FAQ · ARCHIVE INDEX · USER OPTIONS · **iNi Provenance** · **RTFM**
- **USER OPTIONS** — Manual Library, Install, Map, Logs, System, Setup Master, Exit
- **Account gate** — optional; guests can browse.

```text
uXu  ← site · repo · app
└── 0?0.uXu.0000  ← archive interface
    ├── Registry
    ├── Quick Nav (ABOUT · FAQ · INDEX · USER OPTIONS · iNi · RTFM)
    ├── Transparency
    └── Relationships (*:*)
```

`*:*` means connected — not endorsement, ownership, or control.

On the interface: boot lands on Quick Nav · `↑↓` move · `Enter` run · `←→` zones ·
`r` registry · `q` Quick Nav · `/` command · `?` map. Long docs open at the **top**.

## iNi (opt-in provenance)

**iNi** is the inner-circle provenance ethic beside uXu — an “I and I” space
where creators who choose it treat origin, authorship, lineage, and custody as
first-class data. It is not required to publish on uXu.

In the archive interface: Quick Nav **iNi Provenance** (or type `INI`).
Inside that panel: **see website** → https://soloist.ai/uxu (or type `INI SITE`).
Hover tip: *join our community*. Pamphlet: `INI-PROVENANCE.md` / MANUAL LIBRARY.

## How and why it was created

**uXu** exists so archives can stay **independent** and still be **findable**.
Collections should not have to vanish into private drives, dead links, or
platforms that own the shelf more than the librarian does.

The first non-root room was **CyberCat Sunflower**. **0?0** is the interface at
the center: look things up, open a door, read a manual, leave a trail.

Design bets:

- Forever ids — history should not vanish when access does  
- Simple first — folder, page, list; complexity optional  
- Keyboard-honest archive interface — a tool, not only a brochure  
- Share **uXu**; work inside **0?0**  
- Legal & kind — only share what you have the right to share  

*built to archive. tuned to overachieve.*

Human contact: **rasip@chloreform.org** — Victor Birkle.

## License & rights

- Repository software and original docs: **MIT** — see root `LICENSE`.
- Third-party media linked by archives: not owned by uXu; keep source terms.
- Registration ≠ ownership. Optional **iNi** provenance is self-attestation.

**Live:** https://rasvibir.github.io/uXu/  
**Repo:** https://github.com/RASvibir/uXu
