# ini runtime

Runtime app for the root archive interface inside the uXu repository.
This directory is the app/runtime root for Neon, Prisma, environment files,
and optional Cloudflare deployment. Archive content remains in /archives.
